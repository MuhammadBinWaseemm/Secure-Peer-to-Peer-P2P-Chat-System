# Secure-Peer-to-Peer-P2P-Chat-System
SecurePeer is a secure peer-to-peer (P2P) messaging application that allows two users to communicate directly without relying on a central server. The system establishes a direct TCP connection between peers and implements end-to-end encryption to ensure that only the communicating users can read the messages.
